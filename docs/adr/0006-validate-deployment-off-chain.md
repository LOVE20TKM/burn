# 通过部署校验脚本验收部署地址

社区权重、`base`、协议依赖一致性、Factory 列表、周期参数和 gas 上限均由部署校验脚本验收，不为这些运营判断增加管理员配置或链上校验状态。脚本在目标链 fork 上模拟部署、最坏批量销毁和主要聚合 view；部署后再读取冻结配置并与独立基准比较。脚本必须 fail-closed：每项失败都累计失败数，最终以非零状态退出，不能只打印告警。最小验收通过故意传入错误期望值验证退出码。任一检查失败的 Burn 地址视为无效部署，不公布、不接入前端或 BSC 空投流程。
