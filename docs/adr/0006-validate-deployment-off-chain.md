# 通过部署校验脚本验收部署地址

范围代币、显式社区名单及权重、`scoreBase`、协议依赖一致性、Factory 列表、周期参数、可选同链空投代币行为和 gas 上限均由部署校验脚本验收，不为这些运营判断增加管理员配置或链上校验状态。社区权重的计算方法由部署者决定，脚本只按本次部署方案复核构造参数。脚本在目标链 fork 上模拟部署、最坏批量销毁、顺序领取和主要聚合 view，并以 fail-closed 方式累计所有失败后返回非零退出码。任一检查失败的 Burn 地址视为无效部署，不公布、不接入前端或外部空投流程。
