# 部署时冻结参与社区

本次销毁空投仅接受 Burn 部署时已经完成公平发射、社区权重大于零的 LOVE20 根币和以 LOVE20 为父币的直接子币。部署后的新币、尚未完成公平发射的代币、零权重社区以及孙币和更深层代币均不参与，使代币集合与社区权重能够同时冻结且无需后续管理入口，并避免用户在最终份额必为零的社区不可逆销毁资产。`communityWeight` 对非参与社区返回零；所有销毁入口和详细状态、累计数据、份额查询统一回滚 `UnsupportedCommunity`，避免错误地址被误读为有效社区的空数据。
