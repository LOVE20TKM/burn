# 部署时冻结参与社区

本次销毁只接受构造参数显式配置的范围代币社区和已经完成发射的直接子币社区。未配置的直接子币、部署后新完成发射的直接子币、范围代币的孙币和更深层代币均不参与，使代币集合与社区权重同时冻结且无需管理入口，并避免用户在最终份额必为零的社区不可逆销毁资产。`communityWeight` 对非参与社区返回零；所有销毁入口和详细状态、累计数据、份额查询统一回滚 `UnsupportedCommunity`，避免错误地址被误读为有效社区的空数据。
