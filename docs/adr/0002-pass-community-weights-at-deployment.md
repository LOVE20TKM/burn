# 部署时传入并冻结社区权重

Burn 构造函数通过 `CommunityWeight(tokenAddress, weight)[]` 接收本次支持的社区及无单位正整数相对权重，不再读取部署区块的 SL、LP、储备或价格自动计算。合约要求范围代币出现一次，其他配置项必须是已经完成发射的直接子币，并拒绝零权重和重复代币；未配置社区不参与。权重如何得出由部署者决定并在部署流程中复核，不属于合约规则，使部署区块的短期流动性变化不会自动改变链上份额配置。
