# 使用固定竞争份额

每个社区的四类名义份额不因实际销毁总量缩放，只在有正销毁得分的地址之间按比例分配。允许用极小数量进入竞争池，因为公开销毁会让其他有资格地址及时加入并稀释先行者；单一有效参与者获得整池份额是该博弈的预期结果。无人销毁的类别不参与分配，社区总份额按构造时冻结的类别基础权重在活跃类别之间重新归一化；`1:1:1:1` 配置保持原有等分结果，其他配置按相对权重分配。合约不设置类别激活截止，明确接受最后区块仍可首次激活类别并改变社区类别分配的结果，依赖公开状态和参与者主动观察形成竞争。
