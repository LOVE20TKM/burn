# 使用 1e18 精度并向下取整

得分系数、销毁得分和最终份额统一使用 `1e18` 定点精度，乘除使用 `Math.mulDiv` 并向下取整。地址在同一社区类别轮次内分次操作时，根据新旧累计数量对应的轮次得分差值增加得分，使拆单与单次操作结果一致；最终份额舍入余量不分配。同链空投数量也向下取整，但先前领取留下的最小单位余量仍在动态池余额中，可能由后续领取者分得；份额舍入对应的剩余比例始终保留，Burn 不执行末位补差。
